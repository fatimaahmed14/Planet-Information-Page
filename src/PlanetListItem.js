import { Link } from "react-router-dom";

function PlanetListItem(props) {
  const { planets } = props;

  return <li></li>;
}
export default PlanetListItem;

// need to put jsx + html for rendering the fact page in here
// how will it be set out => need to think about this
